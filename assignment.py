# My Python Program
# Task is calculating the Area and Circumference of a circle, using the user's input

import math 
x = float(input ())
y = float(x / 2)
print ("The Area is: ", float(math.pi * (x * 2))
print ("The circumference is: ", float(math.pi * (x**2))
